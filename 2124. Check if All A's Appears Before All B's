class Solution {
    public boolean checkString(String s) {
        int size = s.length();
         for(int i=0;i<size-1;i++)
        {
            if(s.charAt(i)>s.charAt(i+1))
            {
                return false;
            }
        }
        return true;
        
    }
}
